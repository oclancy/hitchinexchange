<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:HitchinExchange.UI">


    <!--DataTemplate DataType="{x:Type local:MessagePropertyDescription}" >
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Label Content="{Binding Name}" />
                <ComboBox ItemsSource="{Binding FieldType.Values}" 
                          Grid.Column="1"
                          DisplayMemberPath="DisplayValue">
                </ComboBox >
                <TextBox Grid.Column="1">
                    <TextBox.Triggers>
                        <DataTrigger Binding="{Binding FieldType.Values.Count}" Value="0">
                            <Setter Property="Visibility" Value="Visibility.Collapsed"/>
                        </DataTrigger>
                    </TextBox.Triggers>
                </TextBox>
            </Grid>
        
    </DataTemplate-->

</ResourceDictionary>